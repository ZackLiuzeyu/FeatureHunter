% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{eval_with_threshold}
\alias{eval_with_threshold}
\title{/ Evaluate model predictions with a threshold}
\usage{
eval_with_threshold(model, sets)
}
\arguments{
\item{model}{Keras \code{predict()}
A trained Keras model (or any model supporting \code{predict()}).}

\item{sets}{A list of datasets, where each element is itself a list with:
\code{x}: / feature matrix
\code{y}: 0/1/ label vector (0/1)
\code{th}: / numeric threshold for binarization}
}
\value{
\code{sets}
A list (same length as \code{sets}), each element containing:
\code{raw}:
A data.frame with predicted probabilities, binary results, and true labels
\code{met}: \code{metrics_from_preds()} F1ACCRECALL
A named vector of metrics (F1, ACC, RECALL) from \code{metrics_from_preds()}
}
\description{
0/1
For a trained model and a list of datasets, this function generates predictions,
applies a threshold to convert probabilities into class labels (0/1),
and computes evaluation metrics. Returns both raw predictions and metrics.
}
\examples{
\dontrun{
# model dlist 
res <- eval_with_threshold(model, dlist)
str(res[[1]]$met) # 
head(res[[1]]$raw) # 
}
}
\seealso{
\code{\link{metrics_from_preds}}
}
