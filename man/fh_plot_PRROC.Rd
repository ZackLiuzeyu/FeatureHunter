% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fh_plot_PRROC.R
\name{fh_plot_PRROC}
\alias{fh_plot_PRROC}
\title{Plot ROC and PR curves for top models from leaderboard CSV (using precrec for PR)}
\usage{
fh_plot_PRROC(
  n_model = 16,
  top_models_csv = NULL,
  score_index = 1L,
  pick_index = NULL,
  nshow = nshow,
  out_pdf = "top_PR-ROC.pdf"
)
}
\arguments{
\item{n_model}{Integer. Number of leaderboard rows to consider (default 16).}

\item{top_models_csv}{Character or NULL. Path to the leaderboard CSV.
If NULL, a default path is constructed as
file.path("heatmap", sprintf("\%d_top_\%s_models.csv", n_model, score_name)).}

\item{score_index}{Integer. Must be 1, 2, or 3. Selects which score name to
use when building the default CSV filename (1 = "Accuracy",
2 = "Recall", 3 = "F-score").}

\item{pick_index}{Optional integer. If provided, selects exactly that row
(1-based) from the leaderboard instead of the first n_model.}

\item{nshow}{Integer. Number of leaderboard rows (defined when drawing heatmap).}

\item{out_pdf}{Character. Output PDF path (default "top_confusion_curves.pdf").}
}
\value{
(Invisibly) the output PDF path.
}
\description{
Reads a leaderboard CSV, selects models (either the first \code{n_model}
entries or a specific \code{pick_index}), then for each model draws ROC and
PR curves across available datasets using prediction probabilities stored in
\code{all_result_summary}. Dataset display names are derived from \code{exp_list}.
PR curves and AP are computed via \code{precrec} for consistency with standard practice.
Outputs a multi-page PDF with one model per page (left: ROC, right: PR).
}
