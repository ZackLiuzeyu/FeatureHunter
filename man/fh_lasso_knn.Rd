% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fh_knn.R
\name{fh_lasso_knn}
\alias{fh_lasso_knn}
\title{LASSO + KNN Interface}
\usage{
fh_lasso_knn(
  train_exp,
  train_labels,
  test_exp,
  labels_list,
  lassogene,
  fold,
  knumber = c(1, 2, 3, 4, 5),
  cutoff = c(0.25, 0.5, 0.75),
  auto_th_method = "auto",
  auto_imbalance_thresh = 0.35,
  auto_pr_vs_roc_gate = 0.5,
  collector = collector
)
}
\arguments{
\item{train_exp}{Training expression matrix.}

\item{train_labels}{Labels (0/1).}

\item{test_exp}{A list: first=B, others=C/D…}

\item{labels_list}{Label data frames (A=1, B=2…).}

\item{lassogene}{Selected feature names from LASSO.}

\item{fold}{Integer used in result key only.}

\item{knumber}{Vector of k values (default \code{c(1,2,3,4,5)}).}

\item{cutoff}{Fixed cutoff values (default \code{c(0.25,0.5,0.75)}).}

\item{auto_th_method}{"auto"/"f1"/"youden".}

\item{auto_imbalance_thresh}{imbalance threshold (default 0.35).}

\item{auto_pr_vs_roc_gate}{PR-vs-ROC gate (default 0.5).}

\item{collector}{Environment for results.}
}
\value{
invisible list of results (acc, recall, fs, summary).
}
\description{
Uses only the LASSO-selected features (\code{lassogene}) to train KNN classifiers.
Thresholds optimized on validation set B (F1/Youden/auto) and merged with fixed cutoffs.
Metrics (F1, ACC, REC) computed on A/B/C/D… and results stored in \code{collector}.
}
\examples{
\dontrun{
collector <- new.env()
fh_lasso_knn(train_exp,train_labels,list(exp_list[[2]],exp_list[[3]]),labels_list,
             lassogene=lassogene,fold=10,knumber=c(1,3,5),collector=collector)
}

}
