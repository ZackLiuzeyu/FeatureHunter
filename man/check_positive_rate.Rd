% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{check_positive_rate}
\alias{check_positive_rate}
\title{Check positive rate of label sets}
\usage{
check_positive_rate(labels_list)
}
\arguments{
\item{labels_list}{2 \code{"positive"} / \code{"1"} / \code{1}
A list of data frames or matrices, each with at least two columns. The 2nd
column is treated as labels. Values \code{"positive"}, \code{"1"}, or numeric \code{1}
are mapped to positives; all others are treated as negatives.}
}
\value{
A data.frame with columns:
\code{Dataset}: Set_i / dataset name (auto-generated Set_i)
\code{Total}: / total number of samples
\code{Positive}: / number of positives
\code{Negative}: / number of negatives
\code{PosRate}: / positive rate (rounded to 4 decimals)
}
\description{
For each dataset in \code{labels_list}, this function computes the positive rate.
It assumes the second column contains labels, which are coerced into binary
(0/1). A summary table is returned with total count, positives, negatives,
and positive rate.
}
\examples{
labs1 <- data.frame(ID=1:5, Label=c("positive","negative","1","0",1))
labs2 <- data.frame(ID=1:4, Label=c(0,0,1,"positive"))
check_positive_rate(list(labs1, labs2))
}
